Generate and open the Claude Token Dashboard in your browser.

Run `python3 ~/.claude/hooks/dashboard.py` to generate the HTML report and open it automatically.

The dashboard shows:
- Total token usage and cost overview
- Monthly usage trends
- Model distribution (Opus/Sonnet/Haiku)
- Project usage ranking
